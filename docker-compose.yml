version: '2'
services:
  whisper-api:
    build:
      context: .
      dockerfile: Dockerfile
    labels:
      lagoon.type: basic-persistent
      lagoon.persistent: /media_root
      lagoon.service.port: 8000
      lagoon.autogeneratedroute: true
    environment:
      MEDIA_ROOT: /media_root
    volumes:
      - ./data:/data:delegated
      - C:/Users/lucas/Documents/Ateleris/Bazar/temp:/media_root
    ports:
      - "8000:8000"
    platform: linux/amd64
